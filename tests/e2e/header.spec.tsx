import * as React from "react";
import {render,cleanup} from "@testing-library/react";
// import Home from "@components/Home";

afterEach(cleanup);
describe('header头部组件测试',() => {
  it('index', () => {
    
  });
});